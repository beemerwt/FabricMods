{
  "schemaVersion": 1,
  "id": "spawnertweaks",
  "version": "${version}",
  "name": "SpawnerTweaks",
  "environment": "server",
  "entrypoints": {
    "server": [ "com.github.beemerwt.spawnertweaks.SpawnerTweaks" ]
  },
  "mixins": [{
    "config": "spawnertweaks.mixins.json",
    "environment": "server"
  }],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
