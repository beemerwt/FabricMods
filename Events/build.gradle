plugins {
    id("fabric-loom")
    id("java")
}

group = project.maven_group
version = project.mod_version

base {
    archivesName = 'events'
}

loom {
    mixin {
        defaultRefmapName = "events.refmap.json"
    }
}