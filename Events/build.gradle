plugins {
    id("fabric-loom")
    id("java")
}

group = 'com.github.beemerwt'
version = project.mod_version

base {
    archivesName = 'events'
}

loom {
    mixin {
        defaultRefmapName = "events.refmap.json"
    }
}