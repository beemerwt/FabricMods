{
  "schemaVersion": 1,
  "id": "quieter",
  "version": "${version}",
  "name": "Quieter",
  "environment": "server",
  "entrypoints": {
    "server": [ "com.github.beemerwt.quieter.Quieter" ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}",
    "resourcelib": ">=1.0.0"
  }
}
