{
  "schemaVersion": 1,
  "id": "fakeplayer",
  "version": "${version}",
  "name": "FakePlayer",
  "description": "Server-only fake player spawner and command runner",
  "authors": ["beemerwt"],
  "license": "MIT",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.github.beemerwt.fakeplayer.FakePlayer"
    ]
  },
  "mixins": [ "fakeplayer.mixins.json" ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
